#!/bin/bash

# ==========================================
# Setup & Configuration
# ==========================================

# strict mode: exit on error, undefined vars, or pipe failures
set -euo pipefail

# Global Constants
readonly DEV_HOME="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

readonly FONT_DIR="$HOME/.local/share/fonts"
readonly TMUX_CONFIG_DIR="$HOME/.config/tmux"

# Colors for logging
readonly END='\e[0m'
readonly GREEN='\e[32m'
readonly BLUE='\e[34m'
readonly PURPLE='\e[35m'
readonly YELLOW='\e[33m'
readonly RED='\e[31m'
readonly GRAY='\e[90m'
readonly WHITE='\e[97m'
readonly BRIGHT_YELLOW='\e[93m'

# ==========================================
# Utility Functions
# ==========================================

link_resource() {

 local src="$1"
 local dest="$2"

 local dest_dir=$(dirname "$dest")
 mkdir -p "$dest_dir"

 if [[ -e "$dest" || -L "$dest" ]]; then
  rm -rf "$dest"
 fi

 ln -s "$src" "$dest"
 echo -e "${WHITE}Linked${END}: ${BLUE}$dest${END} -> ${GREEN}$src${END}"
}

install_fonts() {

 echo -e "Installing fonts..."

 # Ensure destination directory exists
 mkdir -p "$FONT_DIR"

 echo -e "${GRAY}"
 tar -xvf "$DEV_HOME/fonts.tar" -C "$FONT_DIR"
 echo -e "${END}"

 if command -v fc-cache &>/dev/null; then
  fc-cache -f
  echo -e "${WHITE}Font cache updated.${END}"
 else
  echo -e "${YELLOW}fc-cache not found, attempting to install...${END}"
  if sudo apt install fc-cache &> /dev/null; then
   fc-cache -f
  fi
 fi
}

install_media() {

 echo
 echo -e "${WHITE}Installing media resources...${END}"

 local SRC="$DEV_HOME/media/audio"
 local DST="$HOME/.local/share/audio"
 mkdir -p "$DST"

 # Copy content and fix permissions
 sudo cp -rfT "$SRC" "$DST"
 sudo chown -R "$USER:$USER" "$DST"
 
 echo -e "Audio resources installed to ${GREEN}$DST${END}"
}

setup_tmux() {

 echo -e "${GRAY}Configuring Tmux...${END}"

 local legacy_paths=("$HOME/.tmux" "$HOME/.tmux.conf")

 for path in "${legacy_paths[@]}"; do
  [[ -e "$path" || -L "$path" ]] && rm -rf "$path"
 done

 # Link the main configuration directory
 link_resource "$DEV_HOME/tmux" "$TMUX_CONFIG_DIR"

 # Create metadata file
 echo "$TMUX_CONFIG_DIR" > "$TMUX_CONFIG_DIR/tmux.meta"

 # Initialize TPM (Tmux Plugin Manager)
 local tpm_install_script="$TMUX_CONFIG_DIR/plugins/tpm/bin/install_plugins"

 if [[ -x "$tpm_install_script" ]]; then
  echo -e "Running Tmux Plugin Manager installer..."
  # Running inside a subshell to avoid directory changing side-effects
  ("$tpm_install_script" >/dev/null 2>&1) || log_warn "TPM installer returned non-zero exit code."
 else
  echo -e "${YELLOW}TPM installer not found at $tpm_install_script${END}"
 fi
}

setup_bash() {
 echo -e "Configuring Bash..."

 local dot_files_dir="$DEV_HOME/dot-files"
 local dot_files=("bashrc" "bash_packages" "bash_aliases" "bash_functions" "bash_completions")

 for file in "${dot_files[@]}"; do
  link_resource "$dot_files_dir/$file" "$HOME/.$file"
 done
}

# ==========================================
# Main Execution
# ==========================================

main() {

 echo
 echo -e "${GRAY}Initializing Dev Environment${END}"
 echo

 # Ensure sudo permissions upfront to prevent timeout
 sudo -v

 # Safety check for running Tmux
 if [[ -n "${TMUX:-}" ]]; then
  echo -e "${RED}Terminate Tmux before proceeding${END}"
 fi

 install_fonts
 install_media

 setup_bash
 
 echo -e "Configuring Vim..."
 link_resource "$DEV_HOME/vim" "$HOME/.vim"

 setup_tmux

 echo -e "Reloading shell configuration..."
 source "$HOME/.bashrc" || true

 echo
 echo -e "${GRAY}Setup Complete!${END}"
 echo

 echo -e "${WHITE} ⚠️  Restart Terminal ⚠️  ${END}"
 echo
}

# Execute
main "$@"
